# Clone repository
git clone https://github.com/PriyaSarja/Demo.git

# Go to repository folder
cd Demo

# Configure git
git config --global user.name "PriyaSarja"
git config --global user.email "your_email@gmail.com"

# Check status
git status

# Create file
touch demo.txt

# Add and commit
git add demo.txt
git commit -m "initial commit"
git push origin main

# Edit file in main branch
nano demo.txt
# type: Hello from MAIN branch
git add demo.txt
git commit -m "main change"

# Create new branch
git branch feature

# Switch to feature branch
git switch feature

# Edit same line in feature branch
nano demo.txt
# type: Hello from FEATURE branch
git add demo.txt
git commit -m "feature change"

# Switch back to main
git switch main

# Merge feature branch (conflict will raise)
git merge feature

# Open file and resolve conflict
nano demo.txt

# remove:
# <<<<<<< HEAD
# Hello from MAIN branch
# =======
# Hello from FEATURE branch
# >>>>>>> feature

# keep final line like:
# Hello from MAIN and FEATURE branch

# Mark resolved
git add demo.txt

# Commit resolved conflict
git commit -m "resolved merge conflict"

# Push final code
git push origin main
